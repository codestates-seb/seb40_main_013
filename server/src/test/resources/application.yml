spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG # 로그 단계 설정

cloud:
  aws:
    region: # AWS Region 구성
      static: ap-northeast-2
    stack: # EC2에서 Spring Cloud 프로젝트를 실행시키면 기본으로 CloudFormation 구성을 시작
      auto: false

iam:
  access-key: ${S3_ACCESS_KEY}
  secret-key: ${S3_SECRET_KEY}

jwt:
  secret-key: ${JWT_SECRET_KEY}
  access-token-expiration-minutes: 30
  refresh-token-expiration-minutes: 420

env: local

jpa:
  properties:
    hibernate.default_batch_fetch_size: 1000

file:
  profileImg: C:\Users\KKG\Documents\GitHub\seb40_main_013\server\img\profileImg\
  productImg: C:\Users\KKG\Documents\GitHub\seb40_main_013\server\img\productImg\

server:
  servlet:
    encoding:
      force-response: true