spring:
  servlet:
    multipart:
      max-request-size: 10MB
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}
    ttl: ${REDIS_TTL}
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://dailydaily.cvz0lwzweyo3.ap-northeast-2.rds.amazonaws.com:13306/dailydaily
    username: admin
    password: ${RDS_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update


file:
  profileImg: /img/profileImg/
  productImg: /img/productImg/
  productContentsImg: /img/productContentsImg/
  reviewImg: /img/reviewImg/

env: server